# Dify插件调试配置
# 安装方式: local或remote
INSTALL_METHOD=remote

# 远程安装主机
REMOTE_INSTALL_HOST=localhost

# 远程安装端口
REMOTE_INSTALL_PORT=5003

# 远程安装密钥（从Dify平台获取）
REMOTE_INSTALL_KEY=your_dify_debug_key_here

# Spring后端服务URL（默认值）
SPRING_APP_URL=http://localhost:8080 